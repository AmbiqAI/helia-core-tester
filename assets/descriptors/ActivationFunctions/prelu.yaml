operator: PReLU
name: prelu_nhwc_s8
operand_sign_span_exempt:
  alpha: PReLU's alpha is the positive slope constant baked into the TFLite model; steering it would leave the reference interpreter using the model's copy, and the kernel branches on the sign of the input, not of alpha (hct#81)
activation_dtype: S8
weight_dtype: S8
activation: NONE
hint:
  call_style: per_tensor
input_shape: [1, 2, 2, 3]
alpha: 0.25
---
operator: PReLU
name: prelu_vector_s8
operand_sign_span_exempt:
  alpha: PReLU's alpha is the positive slope constant baked into the TFLite model; steering it would leave the reference interpreter using the model's copy, and the kernel branches on the sign of the input, not of alpha (hct#81)
activation_dtype: S8
weight_dtype: S8
activation: NONE
hint:
  call_style: per_tensor
input_shape: [1, 8]
alpha: 0.25
---
operator: PReLU
name: prelu_default_alpha_s8
operand_sign_span_exempt:
  alpha: PReLU's alpha is the positive slope constant baked into the TFLite model; steering it would leave the reference interpreter using the model's copy, and the kernel branches on the sign of the input, not of alpha (hct#81)
activation_dtype: S8
weight_dtype: S8
activation: NONE
hint:
  call_style: per_tensor
input_shape: [1, 2, 2, 3]
---
operator: PReLU
name: prelu_scalar_alpha_s8
activation_dtype: S8
weight_dtype: S8
activation: NONE
hint:
  call_style: per_tensor
input_shape: [1, 2, 2, 1]
alpha: 0.1
alpha_shape: [1]
---
operator: PReLU
name: prelu_broadcast_batch_s8
operand_sign_span_exempt:
  alpha: PReLU's alpha is the positive slope constant baked into the TFLite model; steering it would leave the reference interpreter using the model's copy, and the kernel branches on the sign of the input, not of alpha (hct#81)
activation_dtype: S8
weight_dtype: S8
activation: NONE
hint:
  call_style: per_tensor
input_shape: [2, 2, 2, 3]
alpha_shape: [1, 2, 2, 3]
alpha: 0.2
---
operator: PReLU
name: prelu_broadcast_channel_s8
operand_sign_span_exempt:
  alpha: PReLU's alpha is the positive slope constant baked into the TFLite model; steering it would leave the reference interpreter using the model's copy, and the kernel branches on the sign of the input, not of alpha (hct#81)
activation_dtype: S8
weight_dtype: S8
activation: NONE
hint:
  call_style: per_tensor
input_shape: [1, 2, 2, 3]
alpha_shape: [1, 1, 1, 3]
alpha: 0.15
---
operator: PReLU
name: prelu_row_scalar_alpha_s8
activation_dtype: S8
weight_dtype: S8
activation: NONE
hint:
  call_style: per_tensor
input_shape: [1, 2, 3, 2]
alpha_shape: [1, 2, 1, 1]
alpha: 0.2
---
operator: PReLU
name: prelu_pixel_input_c1_s8
activation_dtype: S8
weight_dtype: S8
activation: NONE
hint:
  call_style: per_tensor
input_shape: [1, 1, 2, 1]
alpha_shape: [1, 1, 2, 1]
alpha: 0.1
---
operator: PReLU
name: prelu_pixel_alpha_c1_s8
activation_dtype: S8
weight_dtype: S8
activation: NONE
hint:
  call_style: per_tensor
input_shape: [1, 1, 2, 3]
alpha_shape: [1, 1, 2, 1]
alpha: 0.1
---
operator: PReLU
name: prelu_scalar_alpha_mixed_sign_s8
operand_sign_span_exempt:
  input: pinned symmetric sign sweep whose four values are the case; it has no lane at the zero boundary by construction, and the generated-data PReLU cases carry that lane instead (hct#81)
  alpha: PReLU's alpha is the positive slope constant baked into the TFLite model; steering it would leave the reference interpreter using the model's copy, and the kernel branches on the sign of the input, not of alpha (hct#81)
activation_dtype: S8
weight_dtype: S8
activation: NONE
hint:
  call_style: per_tensor
  extras:
    input_values: [-0.75, -0.25, 0.25, 0.75]
input_shape: [1, 1, 1, 4]
alpha_shape: [1]
alpha: 0.2
---
operator: PReLU
name: prelu_scalar_alpha_all_negative_input_s8
operand_sign_span_exempt:
  input: pinned uniformly negative input to hold the alpha branch on every lane (hct#81)
  alpha: PReLU's alpha is the positive slope constant baked into the TFLite model; steering it would leave the reference interpreter using the model's copy, and the kernel branches on the sign of the input, not of alpha (hct#81)
activation_dtype: S8
weight_dtype: S8
activation: NONE
hint:
  call_style: per_tensor
  extras:
    input_values: [-0.7, -0.6, -0.5, -0.4]
input_shape: [1, 1, 1, 4]
alpha_shape: [1]
alpha: 0.2
---
operator: PReLU
name: prelu_scalar_alpha_all_positive_input_s8
operand_sign_span_exempt:
  input: pinned uniformly positive input to hold the identity branch on every lane (hct#81)
  alpha: PReLU's alpha is the positive slope constant baked into the TFLite model; steering it would leave the reference interpreter using the model's copy, and the kernel branches on the sign of the input, not of alpha (hct#81)
activation_dtype: S8
weight_dtype: S8
activation: NONE
hint:
  call_style: per_tensor
  extras:
    input_values: [0.3, 0.4, 0.5, 0.6]
input_shape: [1, 1, 1, 4]
alpha_shape: [1]
alpha: 0.2
---
operator: PReLU
name: prelu_arg_error_output_mismatch_s8
activation_dtype: S8
weight_dtype: S8
activation: NONE
hint:
  call_style: per_tensor
input_shape: [1, 2, 2, 3]
alpha_shape: [1, 2, 2, 3]
output_shape: [1, 2, 2, 2]
alpha: 0.2
expected_status: ARM_CMSIS_NN_ARG_ERROR
---
operator: PReLU
name: prelu_batch_row_nobcast_s8
operand_sign_span_exempt:
  alpha: PReLU's alpha is the positive slope constant baked into the TFLite model; steering it would leave the reference interpreter using the model's copy, and the kernel branches on the sign of the input, not of alpha (hct#81)
activation_dtype: S8
weight_dtype: S8
activation: NONE
hint:
  call_style: per_tensor
input_shape: [2, 2, 3, 4]
alpha_shape: [1, 2, 3, 4]
alpha: 0.2

---
operator: PReLU
name: prelu_nhwc_s16
operand_sign_span_exempt:
  alpha: PReLU's alpha is the positive slope constant baked into the TFLite model; steering it would leave the reference interpreter using the model's copy, and the kernel branches on the sign of the input, not of alpha (hct#81)
activation_dtype: S16
weight_dtype: S8
activation: NONE
hint:
  call_style: per_tensor
input_shape: [1, 2, 2, 3]
alpha: 0.25
---
operator: PReLU
name: prelu_vector_s16
operand_sign_span_exempt:
  alpha: PReLU's alpha is the positive slope constant baked into the TFLite model; steering it would leave the reference interpreter using the model's copy, and the kernel branches on the sign of the input, not of alpha (hct#81)
activation_dtype: S16
weight_dtype: S8
activation: NONE
hint:
  call_style: per_tensor
input_shape: [1, 8]
alpha: 0.25
---
operator: PReLU
name: prelu_default_alpha_s16
operand_sign_span_exempt:
  alpha: PReLU's alpha is the positive slope constant baked into the TFLite model; steering it would leave the reference interpreter using the model's copy, and the kernel branches on the sign of the input, not of alpha (hct#81)
activation_dtype: S16
weight_dtype: S8
activation: NONE
hint:
  call_style: per_tensor
input_shape: [1, 2, 2, 3]
---
operator: PReLU
name: prelu_scalar_alpha_s16
activation_dtype: S16
weight_dtype: S8
activation: NONE
hint:
  call_style: per_tensor
input_shape: [1, 2, 2, 1]
alpha: 0.1
alpha_shape: [1]
---
operator: PReLU
name: prelu_broadcast_channel_s16
operand_sign_span_exempt:
  alpha: PReLU's alpha is the positive slope constant baked into the TFLite model; steering it would leave the reference interpreter using the model's copy, and the kernel branches on the sign of the input, not of alpha (hct#81)
activation_dtype: S16
weight_dtype: S8
activation: NONE
hint:
  call_style: per_tensor
input_shape: [1, 2, 2, 3]
alpha_shape: [1, 1, 1, 3]
alpha: 0.15
---
operator: PReLU
name: prelu_scalar_alpha_mixed_sign_s16
operand_sign_span_exempt:
  input: pinned symmetric sign sweep whose four values are the case; it has no lane at the zero boundary by construction, and the generated-data PReLU cases carry that lane instead (hct#81)
  alpha: PReLU's alpha is the positive slope constant baked into the TFLite model; steering it would leave the reference interpreter using the model's copy, and the kernel branches on the sign of the input, not of alpha (hct#81)
activation_dtype: S16
weight_dtype: S8
activation: NONE
hint:
  call_style: per_tensor
  extras:
    input_values: [-0.75, -0.25, 0.25, 0.75]
input_shape: [1, 1, 1, 4]
alpha_shape: [1]
alpha: 0.2
---
operator: PReLU
name: prelu_row_elementwise_hbcast_s8
operand_sign_span_exempt:
  alpha: PReLU's alpha is the positive slope constant baked into the TFLite model; steering it would leave the reference interpreter using the model's copy, and the kernel branches on the sign of the input, not of alpha (hct#81)
activation_dtype: S8
weight_dtype: S8
activation: NONE
hint:
  call_style: per_tensor
input_shape: [1, 3, 2, 3]
alpha_shape: [1, 1, 2, 3]
alpha: 0.2
---
operator: PReLU
name: prelu_row_elementwise_hbcast_s16
operand_sign_span_exempt:
  alpha: PReLU's alpha is the positive slope constant baked into the TFLite model; steering it would leave the reference interpreter using the model's copy, and the kernel branches on the sign of the input, not of alpha (hct#81)
activation_dtype: S16
weight_dtype: S8
activation: NONE
hint:
  call_style: per_tensor
input_shape: [1, 3, 2, 3]
alpha_shape: [1, 1, 2, 3]
alpha: 0.2
---
operator: PReLU
name: prelu_broadcast_batch_s16
operand_sign_span_exempt:
  alpha: PReLU's alpha is the positive slope constant baked into the TFLite model; steering it would leave the reference interpreter using the model's copy, and the kernel branches on the sign of the input, not of alpha (hct#81)
activation_dtype: S16
weight_dtype: S8
activation: NONE
hint:
  call_style: per_tensor
input_shape: [2, 2, 2, 3]
alpha_shape: [1, 2, 2, 3]
alpha: 0.2
---
operator: PReLU
name: prelu_row_scalar_alpha_s16
activation_dtype: S16
weight_dtype: S8
activation: NONE
hint:
  call_style: per_tensor
input_shape: [1, 2, 3, 2]
alpha_shape: [1, 2, 1, 1]
alpha: 0.2
---
operator: PReLU
name: prelu_pixel_alpha_c1_s16
activation_dtype: S16
weight_dtype: S8
activation: NONE
hint:
  call_style: per_tensor
input_shape: [1, 1, 2, 3]
alpha_shape: [1, 1, 2, 1]
alpha: 0.1
---
operator: PReLU
name: prelu_arg_error_output_mismatch_s16
activation_dtype: S16
weight_dtype: S8
activation: NONE
hint:
  call_style: per_tensor
input_shape: [1, 2, 2, 3]
alpha_shape: [1, 2, 2, 3]
output_shape: [1, 2, 2, 2]
alpha: 0.2
expected_status: ARM_CMSIS_NN_ARG_ERROR
---
operator: PReLU
name: prelu_row_scalar_channel_s8
activation_dtype: S8
weight_dtype: S8
activation: NONE
hint:
  call_style: per_tensor
  extras:
    alpha_values: [-0.9, 0.0, 0.9]
input_shape: [1, 3, 1, 4]
alpha_shape: [1, 3, 1, 1]
---
operator: PReLU
name: prelu_row_scalar_channel_s16
activation_dtype: S16
weight_dtype: S8
activation: NONE
hint:
  call_style: per_tensor
  extras:
    alpha_values: [-0.9, 0.0, 0.9]
input_shape: [1, 3, 1, 4]
alpha_shape: [1, 3, 1, 1]
