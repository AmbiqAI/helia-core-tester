# Float Mean parity cases for arm_nn_mean_f32 / arm_nn_mean_f16
# (ns-cmsis-nn PRs #414 / #412). Same call shape as arm_reduce_sum_f32/f16:
# 4D NHWC input dims + 4D binary axis mask + 4D output dims. The kernels
# accumulate in float32 (the f16 kernel widens and rounds once) and divide
# by the reduction count before the single store, so goldens are
# numpy sum with float32 accumulation divided by the reduction count.
#
# required_kernel_symbols gates each case on the target ns-cmsis-nn
# checkout actually declaring the kernel (per-symbol codegen probe): one
# tester pin serves the f32 branch, the f16 branch, and main (which has
# neither) without generating tests that reference missing symbols.
# Axis picks cover both kernel paths: [3] and [1, 2, 3] flatten to the
# contiguous-suffix fast path (MVE tail predication), [1, 2] and [1] force
# the generic strided walk.
operator: Mean
name: mean_float_axis_c_f32
suite: float
tensor_dtypes:
  input: FP32
  output: FP32
input_shape: [1, 3, 4, 5]
axes: [3]
required_kernel_symbols: [arm_nn_mean_f32]
---
operator: Mean
name: mean_float_axis_hwc_f32
suite: float
tensor_dtypes:
  input: FP32
  output: FP32
input_shape: [1, 8, 8, 16]
axes: [1, 2, 3]
required_kernel_symbols: [arm_nn_mean_f32]
---
operator: Mean
name: mean_float_axis_hw_f32
suite: float
tensor_dtypes:
  input: FP32
  output: FP32
input_shape: [1, 8, 8, 16]
axes: [1, 2]
required_kernel_symbols: [arm_nn_mean_f32]
---
operator: Mean
name: mean_float_axis_h_f32
suite: float
tensor_dtypes:
  input: FP32
  output: FP32
input_shape: [1, 3, 4, 5]
axes: [1]
required_kernel_symbols: [arm_nn_mean_f32]
---
operator: Mean
name: mean_float_axis_c_f16
suite: float
tensor_dtypes:
  input: FP16
  output: FP16
input_shape: [1, 3, 4, 5]
axes: [3]
required_kernel_symbols: [arm_nn_mean_f16]
---
operator: Mean
name: mean_float_axis_hwc_f16
suite: float
tensor_dtypes:
  input: FP16
  output: FP16
input_shape: [1, 8, 8, 16]
axes: [1, 2, 3]
required_kernel_symbols: [arm_nn_mean_f16]
---
operator: Mean
name: mean_float_axis_hw_f16
suite: float
tensor_dtypes:
  input: FP16
  output: FP16
input_shape: [1, 8, 8, 16]
axes: [1, 2]
required_kernel_symbols: [arm_nn_mean_f16]
---
operator: Mean
name: mean_float_axis_h_f16
suite: float
tensor_dtypes:
  input: FP16
  output: FP16
input_shape: [1, 3, 4, 5]
axes: [1]
required_kernel_symbols: [arm_nn_mean_f16]
---
# Non-finite cover for the float Mean kernels. The arm_reduce_sum_f32 and
# arm_nn_mean_f32 doxygen blocks in ns-cmsis-nn Include/arm_nnfunctions_flt.h both
# state "NaN and Inf propagate" (the f16 entries @copydoc them), so these lanes are
# strict.
#
# One token per case for the strict cases, deliberately. A reduction folds a whole
# group into one output element, so a case carrying both +Inf and -Inf would put them
# in the same group and the golden would be NaN from the (+Inf) + (-Inf) alone -- a
# result about IEEE addition, not about the kernel. With one token the swept group's
# expected value is the token itself and every other group stays finite and fully
# asserted. The two-token pair at the end of this file is the deliberate exception and
# masks that one group instead of pinning it.
#
# The same header warns that the float32 accumulator can itself saturate to +/-Inf
# depending on accumulation order, and that vector and scalar builds may disagree
# when it does. These inputs are drawn from [-1, 1) in groups of 5, so no
# accumulation order can overflow and the only non-finite output is the token's.
operator: Mean
name: mean_float_nonfinite_nan_f32
suite: float
tensor_dtypes:
  input: FP32
  output: FP32
input_shape: [1, 3, 4, 5]
axes: [3]
input_mode: nonfinite_sweep
nonfinite_policy: strict
nonfinite_tokens: [nan]
required_kernel_symbols: [arm_nn_mean_f32]
---
operator: Mean
name: mean_float_nonfinite_inf_f32
suite: float
tensor_dtypes:
  input: FP32
  output: FP32
input_shape: [1, 3, 4, 5]
axes: [3]
input_mode: nonfinite_sweep
nonfinite_policy: strict
nonfinite_tokens: [inf]
required_kernel_symbols: [arm_nn_mean_f32]
---
operator: Mean
name: mean_float_nonfinite_neginf_f32
suite: float
tensor_dtypes:
  input: FP32
  output: FP32
input_shape: [1, 3, 4, 5]
axes: [3]
input_mode: nonfinite_sweep
nonfinite_policy: strict
nonfinite_tokens: [-inf]
required_kernel_symbols: [arm_nn_mean_f32]
---
operator: Mean
name: mean_float_nonfinite_nan_f16
suite: float
tensor_dtypes:
  input: FP16
  output: FP16
input_shape: [1, 3, 4, 5]
axes: [3]
input_mode: nonfinite_sweep
nonfinite_policy: strict
nonfinite_tokens: [nan]
required_kernel_symbols: [arm_nn_mean_f16]
---
operator: Mean
name: mean_float_nonfinite_inf_f16
suite: float
tensor_dtypes:
  input: FP16
  output: FP16
input_shape: [1, 3, 4, 5]
axes: [3]
input_mode: nonfinite_sweep
nonfinite_policy: strict
nonfinite_tokens: [inf]
required_kernel_symbols: [arm_nn_mean_f16]
---
operator: Mean
name: mean_float_nonfinite_neginf_f16
suite: float
tensor_dtypes:
  input: FP16
  output: FP16
input_shape: [1, 3, 4, 5]
axes: [3]
input_mode: nonfinite_sweep
nonfinite_policy: strict
nonfinite_tokens: [-inf]
required_kernel_symbols: [arm_nn_mean_f16]
---
# The AmbiqAI/ns-cmsis-nn#429 shape: +Inf and -Inf inside ONE reduction group, which
# is what the issue reports hanging the f16 reduction at -Ofast. The single-token
# cases above cannot produce it, because a group needs both infinities before the
# fold can reach Inf + (-Inf). Group 0 (the leading five elements of the last axis)
# therefore holds both tokens and its reference is NaN, so this pair is the one place
# the reduction descriptors use nonfinite_policy: mask -- NaN is what IEEE addition
# gives, not something the header contracts about the kernel's fold order. The other
# eleven groups stay finite and fully asserted, and the case asserts SUCCESS and no
# timeout, which is the part that would catch #429.
operator: Mean
name: mean_float_nonfinite_two_token_f32
suite: float
tensor_dtypes:
  input: FP32
  output: FP32
input_shape: [1, 3, 4, 5]
axes: [3]
input_mode: nonfinite_sweep
nonfinite_policy: mask
nonfinite_tokens: [inf, -inf]
required_kernel_symbols: [arm_nn_mean_f32]
---
operator: Mean
name: mean_float_nonfinite_two_token_f16
suite: float
tensor_dtypes:
  input: FP16
  output: FP16
input_shape: [1, 3, 4, 5]
axes: [3]
input_mode: nonfinite_sweep
nonfinite_policy: mask
nonfinite_tokens: [inf, -inf]
required_kernel_symbols: [arm_nn_mean_f16]
