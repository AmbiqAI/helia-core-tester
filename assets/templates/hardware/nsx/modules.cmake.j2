{# modules.cmake -- placeholder until nsx lock/sync writes its own #}
# Generated by helia-core-tester; `nsx lock` / `nsx sync` replace it.
set(NSX_APP_MODULES
{% for name in modules %}
    {{ name }}
{% endfor %}
)
