#ifndef {{ name|upper }}_DEQUANTIZE_H
#define {{ name|upper }}_DEQUANTIZE_H

#include <stdint.h>
#include <float.h>
// A float16 input may carry NAN/INFINITY, and this header is included ahead of any
// other translation-unit include that would define them.
#include <math.h>
#include "arm_nnfunctions.h"
#include "arm_nn_types.h"

// Input data (quantized, for testing)
static const {{ input_dtype }} {{ name }}_input[] = {
{{ input_data_array }}
};

// Expected output (golden - float32)
static const {{ output_dtype }} {{ name }}_expected_output[] = {
{{ expected_output_array }}
};

#endif
